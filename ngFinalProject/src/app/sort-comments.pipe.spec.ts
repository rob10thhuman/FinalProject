import { SortCommentsPipe } from './sort-comments.pipe';

describe('SortCommentsPipe', () => {
  it('create an instance', () => {
    const pipe = new SortCommentsPipe();
    expect(pipe).toBeTruthy();
  });
});
