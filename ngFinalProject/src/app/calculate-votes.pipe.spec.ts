import { CalculateVotesPipe } from './calculate-votes.pipe';

describe('CalculateVotesPipe', () => {
  it('create an instance', () => {
    const pipe = new CalculateVotesPipe();
    expect(pipe).toBeTruthy();
  });
});
